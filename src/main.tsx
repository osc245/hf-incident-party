import { createRoot } from "react-dom/client";
import { IncidentParty } from "@/components/incident-party";

const root = document.getElementById("root");

createRoot(root).render(<IncidentParty />);
